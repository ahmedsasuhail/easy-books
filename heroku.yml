setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
  config:
    EB_SECRET_KEY: easy-books

build:
  docker:
    release:
      dockerfile: Dockerfile
      target: production
    web: Dockerfile
